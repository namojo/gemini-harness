---
name: ${agent_name}
version: ${version}
model: ${model}
tools: ${tools_yaml}
${extra_frontmatter}---

# ${role_title}

## 핵심 역할

${core_role}

## 작업 원칙

${principles}

## 입력/출력 프로토콜

**입력:** ${input_protocol}

**출력:** ${output_protocol}

## 에러 핸들링

${error_handling}

## 자가 검증

작업 완료 후 반드시 다음을 점검한다:

${self_critique_items}

실패 시 해당 항목을 수정 후 재제출한다.

## 왜 이렇게 설계했는가

${rationale}
